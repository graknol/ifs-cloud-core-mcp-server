# Fine-tuning dependencies for Phi-4 with Unsloth
# Install with: pip install -r requirements_finetune.txt

# Core fine-tuning framework
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
torch>=2.1.0
torchvision
torchaudio

# Transformers and related
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.20.0
peft>=0.7.0
trl>=0.7.0
bitsandbytes>=0.41.0

# Additional utilities
scipy
scikit-learn
matplotlib
wandb  # Optional for experiment tracking
tensorboard  # Optional for visualization

# Memory optimization
flash-attn  # Optional, for faster attention (requires CUDA)
triton>=2.0.0  # For optimized kernels
